"use client";
import { FormEvent, useEffect, useState } from "react";
const API="http://127.0.0.1:4317";
type Status={initialized:boolean;anthropicConfigured:boolean;deepgramConfigured:boolean};
type ComponentStatus={ready?:boolean;version?:string|null;detail?:string;error?:string;[key:string]:unknown};
type Preflight={overallReady:boolean;components:Record<string,ComponentStatus>};
export default function AdminSettings({onClose}:{onClose:()=>void}){
 const [status,setStatus]=useState<Status|null>(null),[preflight,setPreflight]=useState<Preflight|null>(null),[message,setMessage]=useState(""),[saving,setSaving]=useState(false),[saved,setSaved]=useState(false),[showKeys,setShowKeys]=useState(false);
 useEffect(()=>{Promise.all([fetch(`${API}/api/admin/status`,{cache:"no-store"}).then(r=>r.json()),fetch(`${API}/api/system/preflight`,{cache:"no-store"}).then(r=>r.json())]).then(([admin,system])=>{setStatus(admin);setPreflight(system)}).catch(()=>setMessage("The secure local service is not running. Restart Depo Pro, then try again."));},[]);
 async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;setSaving(true);setSaved(false);setMessage("");const data=new FormData(form);try{const response=await fetch(`${API}/api/admin/secrets`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({adminCode:data.get("adminCode"),anthropicApiKey:data.get("anthropicApiKey"),deepgramApiKey:data.get("deepgramApiKey"),claudeModel:data.get("claudeModel")})});const body=await response.json();if(!response.ok)throw new Error(body.error);setMessage("Settings saved securely for this Windows user.");setSaved(true);setStatus({initialized:true,anthropicConfigured:!!data.get("anthropicApiKey")||!!status?.anthropicConfigured,deepgramConfigured:!!data.get("deepgramApiKey")||!!status?.deepgramConfigured});form.reset()}catch(error){setMessage(error instanceof Error?error.message:"Unable to save settings.")}finally{setSaving(false)}}
 function rows(name:string,value:ComponentStatus){if("ready" in value)return [{name,value}];return Object.entries(value).map(([child,item])=>({name:`${name}: ${child}`,value:item as ComponentStatus}))}
 return <main className="admin-shell"><header className="intake-topbar"><button className="back-button" onClick={onClose}>← Back to Depo Pro</button><strong>Administrator Settings</strong><span>Local security</span></header><section className="admin-card"><span className="eyebrow">SECURE CONFIGURATION</span><h1>API keys and system readiness</h1><p className="admin-intro">Keys are sent only to the localhost service, encrypted with Windows Data Protection, and never returned to or stored by the browser.</p><div className="security-banner"><span>✓</span><div><strong>Protected for your Windows account</strong><p>The encrypted file cannot be decrypted under a different Windows user. Keys are never displayed after saving.</p></div></div>{preflight&&<section className="preflight"><h2>System readiness</h2><p className={preflight.overallReady?"ready":"attention"}>{preflight.overallReady?"All required components are ready.":"One or more components need attention."}</p><ul>{Object.entries(preflight.components).flatMap(([name,value])=>rows(name,value)).map(({name,value})=><li key={name}><span className={value.ready?"ready":"missing"}>{value.ready?"✓":"!"}</span><strong>{name}</strong><small>{value.version||value.detail||value.error||(value.ready?"Ready":"Not ready")}</small></li>)}</ul></section>}<form onSubmit={save}><label>{status?.initialized?"Administrator access code":"Create administrator access code"}<input name="adminCode" type="password" minLength={8} required autoComplete="current-password" placeholder="At least 8 characters"/><small>Required whenever secrets are changed.</small></label><div className="secret-heading"><h2>Service credentials</h2><button type="button" onClick={()=>setShowKeys(!showKeys)}>{showKeys?"Hide entries":"Show entries"}</button></div><label>Anthropic API key <span className={`config-status ${status?.anthropicConfigured?"ready":""}`}>{status?.anthropicConfigured?"Configured":"Not configured"}</span><input name="anthropicApiKey" type={showKeys?"text":"password"} autoComplete="new-password" placeholder={status?.anthropicConfigured?"Leave blank to keep existing key":"sk-ant-..."}/></label><label>Deepgram API key <span className={`config-status ${status?.deepgramConfigured?"ready":""}`}>{status?.deepgramConfigured?"Configured":"Not configured"}</span><input name="deepgramApiKey" type={showKeys?"text":"password"} autoComplete="new-password" placeholder={status?.deepgramConfigured?"Leave blank to keep existing key":"Paste Deepgram key"}/></label><label>Claude model<input name="claudeModel" defaultValue="claude-sonnet-4-5" spellCheck={false}/><small>Change only when intentionally upgrading the configured Claude model.</small></label>{message&&<p className="admin-message" role="status">{message}</p>}<div className="modal-actions"><button type="button" onClick={onClose}>Cancel</button><button className="primary-button" disabled={saving}>{saving?"Saving securely…":saved?"Saved":"Save Secure Settings"}</button></div></form><aside className="secret-rules"><h3>Safety rules</h3><ul><li>Never paste API keys into notes, case fields, or uploaded documents.</li><li>Use restricted provider keys and rotate them periodically.</li><li>Do not copy the encrypted secrets file to another computer as a backup.</li></ul></aside></section></main>
}
